//Creating stripe webhook used for continous stripe connection and getting stripe account update on real time 
const express=require("express");
const stripe=require("../config/stripe");

const Router= express.Router();

Router.post("/stripe",)
